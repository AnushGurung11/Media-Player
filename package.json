{
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^17.0.8"
  },
  "scripts": {
    "prepare": "husky"
  },

  "lint-staged": {
    "frontend/**/*.{js,jsx}": "npx eslint --fix --config frontend/eslint.config.js",
    "backend/**/*.js": "npx eslint --fix --config backend/eslint.config.js"
  }
}
